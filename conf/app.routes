# microservice specific routes

# individual (nino) routes (made up to test rest url)
POST     /subscribe      controllers.SubscriptionStubController.createSubscription
GET      /subscribe      controllers.SubscriptionStubController.getSubscription